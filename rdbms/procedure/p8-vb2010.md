Programs 8, 9, and 10 in your lab manual are **Visual Basic 6.0 frontend applications connected to an MS Access backend**. Oracle 10g is only used for programs 1 to 7; it is not used for 8, 9, and 10 testing for pull.

---

### 1. Software Needed

* **Database Engine:** **Microsoft Access** (saving files in the older `.mdb` format) or the built-in **Visual Data Manager (VisData)** inside VB.


* **Frontend IDE:** **Microsoft Visual Basic 6.0 (VB6)**.


* **Oracle 10g:** **Not needed** for programs 8, 9, and 10.



---

### 2. Database Files to Create (MS Access)

Before running each program, create the respective database file and table.

| Program | Database File Name & Path | Table Name | Required Columns / Fields |
| --- | --- | --- | --- |
| **8. Library Management** | `E:\lib.mdb`<br> | `book`<br> | `bookid` (Text/Number), `bookname` (Text), `author` (Text), `quan` (Number)

 |
| **9. Marksheet Processing** | `E:\student.mdb`<br> | `st`<br> | `stid` (Number), `stname` (Text), `m1`, `m2`, `m3`, `m4`, `total`, `result`, `percent`<br> |
| **10. Payroll Processing** | `E:\payroll.mdb`<br> | `emp`<br> | `empid` (Integer), `empname` (Text), `salary` (Double), `gross` (Double), `net` (Double)

 |

(Note: If your system doesn't have an `E:\` partition, save them to `D:\` or `C:\` and update the `Data Source=...` path in `Form_Load`.)

---

### 3. Setup Steps in Visual Basic 6.0

1. **Add ADODB Reference:** Required for database operations.
Go to Project → References in the top menu. Scroll down and tick Microsoft ActiveX Data Objects 2.0 Library (or 2.5 / 2.8) and click OK.
Verification: Running the code will no longer throw a "User-defined type not defined" error on Dim cn As New ADODB.Connection.


2. **Configure Data Environment:** Required for generating reports.
Right-click the Project Explorer window → Add → Data Environment. Right-click Connection1 → Properties, choose Microsoft Jet 4.0 OLE DB Provider, click Next, browse to your .mdb file, and click Test Connection. Then right-click Connection1 → Add Command, and set its source to your table.
Verification: A popup displaying "Test connection succeeded" will appear.


3. **Configure Data Report:** Required for report viewer.
Right-click the Project Explorer window → Add → Data Report. In the Properties panel on the right, set:

* DataSource = DataEnvironment1

* DataMember = Command1


Drag the field headers into the report layout.
Verification: Clicking the report button will display the generated preview without runtime property errors.


convert to vb 2010 in step by step process ( like teaching dumb { open ms access go to new > database > .... }) clear step by step process



### Step 1: Create the MS Access Database

1. Open **Microsoft Access** on your computer.
2. Click on **Blank Database**.
3. On the right side of the screen, click the **tiny yellow folder icon** next to the file name. This lets you choose where to save it.
4. Choose your **E:\ drive** (or Desktop if E:\ doesn't exist).
5. In the "Save as type" dropdown at the bottom of that window, you MUST select **Microsoft Access 2002-2003 Database (*.mdb)**. If you don't do this, the connection code won't work.
6. Name the file `lib.mdb` and click **Create**.


7. A default table named "Table1" will appear. Right-click the "Table1" tab at the top and select **Design View**.
8. A box will ask you to save the table. Name it `book` and click **OK**.


9. Create your 4 columns exactly like this:


* Field Name: `bookid` | Data Type: Text
* Field Name: `bookname` | Data Type: Text
* Field Name: `author` | Data Type: Text
* Field Name: `quan` | Data Type: Number


10. Click the **Save** icon at the very top left.
11. **IMPORTANT:** Close Microsoft Access completely. If Access is left open, Visual Basic 2010 will give you a "File is in use" error later.

### Step 2: Create the Visual Basic 2010 Project

1. Open **Microsoft Visual Studio 2010** (or Visual Basic 2010).
2. Go to the top menu and click **File > New > Project**.
3. Select **Windows Forms Application**.
4. Name the project `LibrarySystem` at the bottom and click **OK**.
5. You will now see a blank gray square. This is **Form1**.
6. Open the **Toolbox** on the left side of your screen.
7. Click and drag **4 TextBoxes** onto Form1. (These will be for ID, Name, Author, and Quantity).
8. Click and drag **1 Button** onto Form1. In the Properties window on the bottom right, change the button's `Text` property to "Add Book".

### Step 3: Write the VB 2010 Code (ADO.NET)

Visual Basic 2010 does not use `rs.AddNew` or `ADODB` like your lab manual. It uses a newer system called ADO.NET.

1. **Double-click** your "Add Book" button. This will open the code screen.
2. Delete everything on the screen and copy-paste this exact code:

```vb
' This line tells VB 2010 we are working with databases
Imports System.Data.OleDb

Public Class Form1
    
    ' 1. Set up the connection to the lib.mdb file on your E drive
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Button1_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button1.Click
        Try
            ' 2. Open the database connection
            cn.Open()

            ' 3. Write the SQL command to insert the data from the TextBoxes
            ' TextBox1 = bookid, TextBox2 = bookname, TextBox3 = author, TextBox4 = quan
            Dim sql As String = "INSERT INTO book (bookid, bookname, author, quan) VALUES ('" & TextBox1.Text & "', '" & TextBox2.Text & "', '" & TextBox3.Text & "', " & TextBox4.Text & ")"
            
            Dim cmd As New OleDbCommand(sql, cn)

            ' 4. Run the command to save to the database
            cmd.ExecuteNonQuery()

            ' 5. Show success message just like the lab manual
            MsgBox("Successfully Inserted")
            
            ' 6. Clear the textboxes
            TextBox1.Text = ""
            TextBox2.Text = ""
            TextBox3.Text = ""
            TextBox4.Text = ""

        Catch ex As Exception
            ' If something goes wrong (like a typo), show the error
            MsgBox("Error: " & ex.Message)
        Finally
            ' 7. Always close the connection when finished
            cn.Close()
        End Try
    End Sub

End Class

```

### Step 4: How to view the "Data Report" in VB 2010

VB 2010 does not have the "Data Environment" or "Data Report" tools shown in the lab manual. The modern, standard way to view a database report in VB 2010 is using a **DataGridView**.

1. Go back to your Form designer.
2. Drag a **Button** from the Toolbox onto the form and name it "View Report".
3. Drag a **DataGridView** from the Toolbox onto the form (it looks like a gray spreadsheet).
4. Double-click the "View Report" button and paste this code:

```vb
Private Sub Button2_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button2.Click
    Try
        cn.Open()
        
        ' 1. Grab all the books from the database
        Dim cmd As New OleDbCommand("SELECT * FROM book", cn)
        Dim adapter As New OleDbDataAdapter(cmd)
        Dim table As New DataTable()
        
        ' 2. Fill the table with the data
        adapter.Fill(table)
        
        ' 3. Display it in the DataGridView
        DataGridView1.DataSource = table
        
    Catch ex As Exception
        MsgBox("Error: " & ex.Message)
    Finally
        cn.Close()
    End Try
End Sub

```

### How to adapt this for Programs 9 and 10

You can use this exact same framework for **Student Marksheet (Program 9)** and **Payroll (Program 10)**.

1. Create a new `.mdb` file (`student.mdb` or `payroll.mdb`).


2. Change the `Data Source=E:\lib.mdb` line in the code to match your new file name.


3. Change the `INSERT INTO book...` SQL query to match the columns for the `st` table or the `emp` table.


now others are same for 8th program  form 2,3 ,4 ?? and i didnt understand from view report we view report from form 1 4th button i have to write this {Private Sub Button2_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button2.Click
    Try
        cn.Open()
        
        ' 1. Grab all the books from the database
        Dim cmd As New OleDbCommand("SELECT * FROM book", cn)
        Dim adapter As New OleDbDataAdapter(cmd)
        Dim table As New DataTable()
        
        ' 2. Fill the table with the data
        adapter.Fill(table)
        
        ' 3. Display it in the DataGridView
        DataGridView1.DataSource = table
        
    Catch ex As Exception
        MsgBox("Error: " & ex.Message)
    Finally
        cn.Close()
    End Try
End Sub} to retrive data inside the code editor of that button and what to do how to select that datagridview ?? i didnt get that point 



The code for Forms 2, 3, and 4 is **not** the same in VB 2010. Your lab manual uses an older database technology called `ADODB.Recordset` for those forms, which VB 2010 replaces with `OleDb`.

Also, you are totally right to be confused about the "View Report" button. In your college's VB6 manual, clicking the 4th button opens a `DataReport` screen. Since VB 2010 doesn't have `DataReport`, we will create a **Form 5** specifically to hold the `DataGridView` so it acts just like the popup report in your manual.

Here is the exact step-by-step to build the complete multi-form Program 8 in VB 2010.

1. **Create Form 1 (Main Menu):** The Main Menu.
1. Open your project. You are looking at Form1.
2. Drag 4 Buttons onto Form1. Name them "Add Book", "Issue Book", "Return Book", and "View Report" just like the manual.


3. We need to create the other forms. Go to the very top menu: Project > Add Windows Form. Name it Form2.vb and click Add.
4. Repeat this to create Form3, Form4, and Form5.
5. Go back to Form1. Double-click each button and add this simple code to open the other forms:

```vb
Public Class Form1
    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        Form2.Show() ' Opens Add Book
    End Sub

    Private Sub Button2_Click(sender As Object, e As EventArgs) Handles Button2.Click
        Form3.Show() ' Opens Issue Book
    End Sub

    Private Sub Button3_Click(sender As Object, e As EventArgs) Handles Button3.Click
        Form4.Show() ' Opens Return Book
    End Sub

    Private Sub Button4_Click(sender As Object, e As EventArgs) Handles Button4.Click
        Form5.Show() ' Opens the Report
    End Sub
End Class

```


2. **Create Form 2 (Add Book):** Replaces your old 'rs.AddNew' code.
1. Open Form2 in the designer.
2. Add 4 TextBoxes and 1 Button ("Add").
3. Double-click the "Add" button and use the INSERT code from our previous chat:

```vb
Imports System.Data.OleDb
Public Class Form2
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        Try
            cn.Open()
            Dim sql As String = "INSERT INTO book (bookid, bookname, author, quan) VALUES ('" & TextBox1.Text & "', '" & TextBox2.Text & "', '" & TextBox3.Text & "', " & TextBox4.Text & ")"
            Dim cmd As New OleDbCommand(sql, cn)
            cmd.ExecuteNonQuery()
            MsgBox("Successfully Inserted")
        Catch ex As Exception
            MsgBox(ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub
End Class

```


3. **Create Form 3 (Issue Book):** Subtracts 1 from quantity.
1. Open Form3. In your manual, issuing a book uses a popup InputBox. So you just need 1 Button on this form named "Issue".


2. Double-click the "Issue" button and paste this:

```vb
Imports System.Data.OleDb
Public Class Form3
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        Dim bid As String = InputBox("Enter the Book ID :")
        If bid = "" Then Exit Sub ' Stops if user clicks cancel

        Try
            cn.Open()
            ' This matches the lab manual: update book set quan = quan - 1[cite: 1]
            Dim sql As String = "UPDATE book SET quan = quan - 1 WHERE bookid = '" & bid & "'"
            Dim cmd As New OleDbCommand(sql, cn)
            
            Dim rowsAffected As Integer = cmd.ExecuteNonQuery()
            If rowsAffected > 0 Then
                MsgBox("Book Issued")
            Else
                MsgBox("No record found with ID: " & bid)
            End If
        Catch ex As Exception
            MsgBox(ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub
End Class

```


4. **Create Form 4 (Return Book):** Adds 1 to quantity.
1. Open Form4. Just like Form 3, add 1 Button named "Return".
2. Double-click the "Return" button and paste this (it is exactly the same as Issue, but uses a + 1):



```vb
Imports System.Data.OleDb
Public Class Form4
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        Dim bid As String = InputBox("Enter the Book ID to return book :")
        If bid = "" Then Exit Sub

        Try
            cn.Open()
            ' This matches the lab manual: update book set quan = quan + 1[cite: 1]
            Dim sql As String = "UPDATE book SET quan = quan + 1 WHERE bookid = '" & bid & "'"
            Dim cmd As New OleDbCommand(sql, cn)
            
            Dim rowsAffected As Integer = cmd.ExecuteNonQuery()
            If rowsAffected > 0 Then
                MsgBox("Book Returned")
            Else
                MsgBox("No record found with ID: " & bid)
            End If
        Catch ex As Exception
            MsgBox(ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub
End Class

```


5. **Create Form 5 (View Report):** How to select and use DataGridView.
1. Open Form5. This form will *only* hold your report grid.
2. Look at the Toolbox on the left side of your screen.
3. Scroll down until you see a category named Data. Click the little arrow next to it to expand it.
4. Inside, click on DataGridView.
5. Click anywhere on Form5 to drop it. It will look like a gray empty spreadsheet box. Drag its edges to make it fill the form. By default, its name is DataGridView1.
6. We want the grid to load the database the moment Form5 opens. Double-click anywhere on the empty background of Form5 (do not click the grid itself). This opens the Form_Load code block. Paste this code:

```vb
Imports System.Data.OleDb
Public Class Form5
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Form5_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        Try
            cn.Open()
            Dim cmd As New OleDbCommand("SELECT * FROM book", cn)
            Dim adapter As New OleDbDataAdapter(cmd)
            Dim table As New DataTable()
            
            adapter.Fill(table)
            
            ' This line links the table to the gray box you dragged onto the form
            DataGridView1.DataSource = table
            
        Catch ex As Exception
            MsgBox("Error: " & ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub
End Class

```



ok bro now dont worry about the codes you give me now ,, now convert all code in 8th program to vb2010 version from form1 - form4 (in pdf ) and form5( datagridview) in order

Here is the complete, compiled VB 2010 (ADO.NET) code for the **8th Program (Library Management System)**, adapted directly from the logic and naming conventions used in your lab manual.

### Form 1 (Main Menu)

This form links all the other screens together using your original button names.

```vb
Public Class Form1

    Private Sub addcmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles addcmd.Click
        Form2.Show()
    End Sub

    Private Sub issuecmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles issuecmd.Click
        Form3.Show()
    End Sub

    Private Sub returncmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles returncmd.Click
        Form4.Show()
    End Sub

    Private Sub reportcmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles reportcmd.Click
        Form5.Show()
    End Sub

End Class

```

---

### Form 2 (Add Book)

This replaces the `rs.AddNew` logic with the modern `INSERT INTO` command.

```vb
Imports System.Data.OleDb

Public Class Form2
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub addcmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles addcmd.Click
        Try
            cn.Open()
            ' Maps to Text1 (ID), Text2 (Name), Text3 (Author), Text4 (Quantity)
            Dim sql As String = "INSERT INTO book (bookid, bookname, author, quan) VALUES ('" & Text1.Text & "', '" & Text2.Text & "', '" & Text3.Text & "', " & Text4.Text & ")"
            Dim cmd As New OleDbCommand(sql, cn)
            
            cmd.ExecuteNonQuery()
            
            MsgBox("Successfully Inserted")
            Text1.Text = ""
            Text2.Text = ""
            Text3.Text = ""
            Text4.Text = ""

        Catch ex As Exception
            MsgBox("Error: " & ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub

    Private Sub closecmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles closecmd.Click
        Me.Close()
    End Sub
End Class

```

---

### Form 3 (Issue Book)

This reads the database to find the book, fills the textboxes to show what was found, and updates the quantity by subtracting 1 (`quan=quan-1`).

```vb
Imports System.Data.OleDb

Public Class Form3
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Form3_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load
        ' Disables editing of these fields just like the manual
        Text1.Enabled = False
        Text2.Enabled = False
    End Sub

    Private Sub issuecmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles issuecmd.Click
        Dim bid As String = InputBox("Enter the Book ID :")
        If bid = "" Then Exit Sub

        Try
            cn.Open()
            
            ' First, find the book to display its name in the TextBoxes
            Dim selectSql As String = "SELECT * FROM book WHERE bookid = '" & bid & "'"
            Dim selectCmd As New OleDbCommand(selectSql, cn)
            Dim reader As OleDbDataReader = selectCmd.ExecuteReader()

            If reader.Read() Then
                Text1.Text = reader("bookid").ToString()
                Text2.Text = reader("bookname").ToString()
                reader.Close() ' You must close the reader before running an update

                ' Update the quantity
                Dim updateSql As String = "UPDATE book SET quan = quan - 1 WHERE bookid = '" & bid & "'"
                Dim updateCmd As New OleDbCommand(updateSql, cn)
                updateCmd.ExecuteNonQuery()
                
                MsgBox("Book Issued")
            Else
                MsgBox("No record found with ID: " & bid)
                reader.Close()
            End If

        Catch ex As Exception
            MsgBox("Error: " & ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub

    Private Sub closecmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles closecmd.Click
        Me.Close()
    End Sub
End Class

```

---

### Form 4 (Return Book)

This is nearly identical to Form 3, but it adds 1 to the quantity (`quan=quan+1`) instead of subtracting.

```vb
Imports System.Data.OleDb

Public Class Form4
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Form4_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load
        Text1.Enabled = False
        Text2.Enabled = False
    End Sub

    Private Sub retcmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles retcmd.Click
        Dim bid As String = InputBox("Enter the Book ID to return book :")
        If bid = "" Then Exit Sub

        Try
            cn.Open()
            
            Dim selectSql As String = "SELECT * FROM book WHERE bookid = '" & bid & "'"
            Dim selectCmd As New OleDbCommand(selectSql, cn)
            Dim reader As OleDbDataReader = selectCmd.ExecuteReader()

            If reader.Read() Then
                Text1.Text = reader("bookid").ToString()
                Text2.Text = reader("bookname").ToString()
                reader.Close()

                ' Update the quantity (add 1)
                Dim updateSql As String = "UPDATE book SET quan = quan + 1 WHERE bookid = '" & bid & "'"
                Dim updateCmd As New OleDbCommand(updateSql, cn)
                updateCmd.ExecuteNonQuery()
                
                MsgBox("Book Returned")
            Else
                MsgBox("No record found with ID: " & bid)
                reader.Close()
            End If

        Catch ex As Exception
            MsgBox("Error: " & ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub

    Private Sub closecmd_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles closecmd.Click
        Me.Close()
    End Sub
End Class

```

---

### Form 5 (View Report)

This entirely replaces the `DataReport1` logic. You will place a `DataGridView1` on this form, and the code below will automatically populate it when the form opens.

```vb
Imports System.Data.OleDb

Public Class Form5
    Dim cn As New OleDbConnection("Provider=Microsoft.Jet.OLEDB.4.0;Data Source=E:\lib.mdb")

    Private Sub Form5_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load
        Try
            cn.Open()
            Dim cmd As New OleDbCommand("SELECT * FROM book", cn)
            Dim adapter As New OleDbDataAdapter(cmd)
            Dim table As New DataTable()
            
            adapter.Fill(table)
            
            ' Fills the DataGridView with the database table
            DataGridView1.DataSource = table
            
        Catch ex As Exception
            MsgBox("Error: " & ex.Message)
        Finally
            cn.Close()
        End Try
    End Sub
End Class

```
